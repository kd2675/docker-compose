version: '3.8'
name: subuk-f-a-d

services:
  subuk-f-a-d:
    build:
      context: /data/git/SUBUK-F-A
      dockerfile: Dockerfile
    container_name: subuk-f-a-d
    image: kimd0/subuk-f-a:latest
    networks:
      - network_dev
    ports:
      - 22007:22007
    environment:
      - TZ=Asia/Seoul
      - USE_PROFILE=pm2:prod
    healthcheck:
      test: curl -f http://localhost:8910/api/health || exit 1
      interval: 60s
      retries: 5
      start_period: 20s
      timeout: 10s
networks:
  network_dev:
    external: true